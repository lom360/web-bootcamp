class Point {
    x: number; // We call "variables" of a class fields.
    y: number;

    draw() { // We call a "function" of a class a method.
        //..
    }

    getDistance(another: Point) {
        //..
    }
}

