var log = function (message) {
    console.log(message);
};
// Arrow function is a typescript way to create functions.
var doLog = function (message) {
    console.log(message);
};
// If you only have one line statement in the function
// then you can get rid of the curly braces.
// Below is an example.
// let doLog = (message) => console.log(message);
// If you have no parameters then there will just be an empty paranthesis.
// let doLog = () => console.log();
